%h1 Rentals
%table{class: 'table table-striped'}
  %thead
    %tr
      %th Name
      %th Daily rate
      %th{:colspan => "3"}
  %tbody
    - @rentals.each do |rental|
      %tr
        %td= rental.name
        %td= rental.daily_rate
        %td= link_to 'Show', rental_path(rental.id), class: 'btn btn-info'
        %td= link_to 'Edit', edit_rental_path(rental.id), class: 'btn btn-warning'
        %td= link_to 'Destroy', rental_path(rental.id), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'
-# %br/
= link_to 'New Rental', new_rental_path, class: 'btn btn-primary'
